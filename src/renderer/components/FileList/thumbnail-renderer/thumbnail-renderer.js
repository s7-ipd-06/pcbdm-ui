/**
 * Renders a thumbnail for a file
 * @param   path  (string)  Path to file
 * @return        (string)  Base64 encoded image?
 */
module.exports = (path) => {
  // Check file  type

  // Check whether there is a renderer for this file type

  // Execute the renderer and return the result

  return '';
}
